# --- 1. Fișiere generate de Build System (CMake) ---
# Directorul de build (unde se genereaza Makefile-urile si obiectele)
build/
# Directorul unde sunt plasate librariile si executabilele finale (setate in CMakeLists.txt)
lib/
# Executabilele din directorul Examples (setate in CMakeLists.txt)
Examples/Monocular/mono_kitti
# Fisiere CMake temporare/cache
CMakeCache.txt
CMakeFiles/
cmake_install.cmake

# --- 2. Fisiere generate in timpul Execuției (Profiling) ---
# Acestea sunt output-urile de timp si harta generate la rulare
ExecMean.txt
TrackingTimeStats.txt
LocalMapTimeStats.txt
LBA_Stats.txt
SessionInfo.txt

# Fisiere de traiectorie
CameraTrajectory.txt
KeyFrameTrajectory.txt
PoseGraph.g2o

# --- 3. Fisiere temporare si de Debug ---
*.o        # Fisiere obiect (Object files)
*.d        # Fisiere de dependinte
*.so       # Librarii partajate (Shared libraries)
*.dll      # Librarii Windows
*.a        # Librarii statice

# --- 4. Dependente terte (Thirdparty) care nu trebuie urmarite ---
# Directorul unde se compileaza g2o (daca nu este adaugat ca submodule)
Thirdparty/g2o/build/

# Fisiere de date mari (Vocabularul)
# De obicei, acestea sunt mari si ar trebui distribuite separat, nu in Git.
ORBvoc.txt

# --- 5. Medii Virtuale Python (pentru plot.py) ---
# Ignora directorul venv creat pentru pandas/matplotlib
venv/

# --- 6. Fisiere de configurare IDE/OS ---
.DS_Store     # macOS
.vscode/      # Visual Studio Code
.idea/        # IntelliJ/CLion